<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >
 
    <TextView android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="#ED9A00"
        android:layout_alignParentTop="true"
        android:id="@+id/setupSecondChoose"
        android:padding="10dp"
        android:textSize="25sp"
        android:text="Choose initial categories : " />
    <View android:layout_width="match_parent"
        android:layout_height="2dp"
        android:id="@+id/secondSetupPlaceholder"
        android:layout_below="@id/setupSecondChoose"
        android:background="#A0F7D393"/>

    <Button android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Done"
                android:layout_centerHorizontal="true"
                android:id="@+id/setupSecondNext"
                android:layout_alignParentBottom="true"
                android:onClick="makeChannels" />
    
            <ExpandableListView
                android:id="@+id/mListView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_above="@id/setupSecondNext"
                android:layout_below="@+id/secondSetupPlaceholder" />
 
</RelativeLayout>